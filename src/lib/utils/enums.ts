/** LocalStorage keys used to persist data for the application */
export enum StorageKeys {
	CONFIG_FILE = "custom_yaml_config",
	EXERCISES_PROGRESS = "progression-tracker-v1",
	SESSIONS_LOG = "progression-tracker-sessions-v1",
}
